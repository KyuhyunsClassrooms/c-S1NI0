# 4. 디버깅 보고서 (10점)

> 💡 **디버깅이란?** 프로그램에서 발생한 오류(버그)를 찾아내고 고치는 과정입니다.

---

## 📝 작성 정보
- **작성 날짜:** 2025년 11월 17일
- **작성자:** 오인열

---

## 🐛 발견한 수정점 #1

### 1) 어떤 문제가 발생했나요?
if만을 사용하여 반복해 노가다성 최적화되지 않은 코드가 보임

### 2) 문제가 발생한 코드
```c
char checkWinner() {
    int i;
    
    // 가로 체크
    if (board[0] != ' ' && board[0] == board[1] && board[1] == board[2]) return board[0];
    if (board[3] != ' ' && board[3] == board[4] && board[4] == board[5]) return board[3];
    if (board[6] != ' ' && board[6] == board[7] && board[7] == board[8]) return board[6];
    
    // 세로 체크
    if (board[0] != ' ' && board[0] == board[3] && board[3] == board[6]) return board[0];
    if (board[1] != ' ' && board[1] == board[4] && board[4] == board[7]) return board[1];
    if (board[2] != ' ' && board[2] == board[5] && board[5] == board[8]) return board[2];
    
    // 대각선 체크
    if (board[0] != ' ' && board[0] == board[4] && board[4] == board[8]) return board[0];
    if (board[2] != ' ' && board[2] == board[4] && board[4] == board[6]) return board[2];
    
    // 무승부 체크
    for (i = 0; i < 9; i++) {
        if (board[i] == ' ') {
            return ' '; // 게임 진행 중
        }
    }
    
    return 'D'; // 무승부
}
```

### 3) 왜 오류가 생겼나요?(수정 이유)
```
너무 긴 코드를 보기 편하고 효율적으로 작동하도록 바꿨다.
```

### 4) 어떻게 고쳤나요?
```c
char checkWinner() {
    int lines[8][3] = {
        {0, 1, 2}, {3, 4, 5}, {6, 7, 8},  // 가로
        {0, 3, 6}, {1, 4, 7}, {2, 5, 8},  // 세로
        {0, 4, 8}, {2, 4, 6}              // 대각선
    };
    
    int i;
    for (i = 0; i < 8; i++) {
        int a = lines[i][0];
        int b = lines[i][1];
        int c = lines[i][2];
        
        if (board[a] != ' ' && board[a] == board[b] && board[a] == board[c]) {
            return board[a];
        }
    }
    
    // 무승부 체크
    for (i = 0; i < 9; i++) {
        if (board[i] == ' ') {
            return ' '; // 게임 진행 중
        }
    }
    
    return 'D'; // 무승부
}
```

### 5) 고친 후 결과
- [✅] ✅ 잘 작동해요!
- [ ] ❌ 아직 문제가 있어요

---

## 🧪 프로그램 테스트 결과

### 테스트 1
- **무엇을 테스트했나요?** 틱택토 플레이
- **어떻게 테스트했나요?** 선공, 후공을 정한 후 게임 플레이 (숫자를 입력해 둘 위치를 정함) 
- **결과:** ✅ 성공 

## 💭 디버깅하면서 배운 점

이번 프로젝트에서 오류를 고치면서 새롭게 알게 된 점이나 느낀 점을 자유롭게 적어보세요.

```
- 인공지능을 사용하면 코드를 짜는데 도움을 받을 수 있음을 알게되었다.
- 어떤 기능을 사용하냐에 따라서 코드의 수준이 달라질 수 있음을 꺠달았다.

```

---

## ✅ 최종 체크리스트

프로그램이 잘 작동하는지 최종 확인해보세요!

- [✅] 컴파일이 잘 되나요? (오류 메시지가 없나요?)
- [✅] 프로그램이 실행되나요?
- [✅] 메뉴가 제대로 나오나요?
- [✅] 입력한 값이 정상적으로 처리되나요?
- [✅] 모든 기능이 잘 작동하나요?
